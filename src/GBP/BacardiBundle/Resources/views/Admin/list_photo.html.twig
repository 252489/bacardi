{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    {% if( object.photo ) %}
        <img class="photo" width="100" height="133" src="{{ object.photo }}" alt="{{ object.email}}"/>
    {% endif %}
{% endblock %}